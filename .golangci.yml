version: "2"

linters:
  enable:
    - errcheck
    - funlen
    - lll
    - gocyclo
    - gocognit
    - gocritic

  settings:
    lll:
      line-length: 200
    
    funlen:
      lines: 180
      statements: 180

run:
  timeout: 5m
